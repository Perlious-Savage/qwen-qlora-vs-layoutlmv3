# CPU service and tests. No torch: the API does not need it to start, serve or be tested,
# and CI stays fast enough to be worth running on every push.
pydantic>=2.0
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pytest>=8.0
